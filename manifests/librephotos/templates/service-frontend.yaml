apiVersion: v1
kind: Service
metadata:
  name: {{ include "librephotos.fullname" . }}-frontend
  labels:
    {{- include "librephotos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "librephotos.fullname" . }}-frontend
