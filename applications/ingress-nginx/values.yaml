ingress-nginx:
  controller:
    service:
      # Forces MetalLB to set this IP
      # loadBalancerIP: "192.168.1.240"
      type: ClusterIP

    metrics:
      enabled: true
      serviceMonitor:
        enabled: true
