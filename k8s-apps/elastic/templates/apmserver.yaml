# apiVersion: apm.k8s.elastic.co/v1
# kind: ApmServer
# metadata:
#   name: apm-server
# spec:
#   version: {{ .Values.version }}
#   count: 1
#   elasticsearchRef:
#     name: elastic
#   kibanaRef:
#     name: kibana
