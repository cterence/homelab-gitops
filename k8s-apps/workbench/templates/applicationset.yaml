{{- if not .Values.image }}
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: workbench
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - git:
        repoURL: git@github.com:cterence/homelab-gitops.git
        revision: HEAD
        directories:
          - path: workbench/*
  template:
    metadata:
      name: "build-{{ `{{ .path.basename }}` }}"
    spec:
      project: default
      sources:
        - repoURL: git@github.com:cterence/homelab-gitops.git
          targetRevision: HEAD
          path: k8s-apps/workbench
          helm:
            valueFiles:
              - "$values/{{ `{{ .path.path }}` }}/build.yaml"
            parameters:
              - name: subPath
                value: "{{ `{{ .path.path }}` }}"
        - repoURL: git@github.com:cterence/homelab-gitops.git
          targetRevision: HEAD
          ref: values
      destination:
        name: in-cluster
        namespace: workbench
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
{{- end }}
